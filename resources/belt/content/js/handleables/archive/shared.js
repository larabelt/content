export default {
    data() {
        return {
            entity_type: this.$parent.entity_type,
            entity_id: this.$parent.entity_id,
            table: this.$parent.table,
        }
    },
}